# Digest pipeline settings.
# News sources are managed via admin panel → data/selections.json
# Full source catalog: data/catalog.json

settings:
  max_items_per_source: 5
  max_items_per_country: 4
  max_total_items: 20
  lookback_hours: 168
